language: python
python:
    - "2.7"
before_install:
    - ls -al
    - pwd
    - git clone --branch 0.3.0 https://github.com/blue-button/bluebutton.js
    - "bash -c 'cd bluebutton.js; npm install; bower install; grunt'"
    - ls bluebutton.js/sample_ccdas
install:
    - ls -al
    - pwd
    - pip install -e .
script:
    - make test
