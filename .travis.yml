language: python
python:
    - "2.7"
before_install:
    - git clone https://github.com/blue-button/bluebutton.js
    - git checkout 0.3.0
    - pushd bluebutton.js
    - npm install
    - bower install
    - grunt
    - popd
install:
    - pip install -e .
script:
    - make test || python setup.py nosetests
